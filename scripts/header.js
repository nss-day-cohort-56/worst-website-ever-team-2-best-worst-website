// make function that returns html for the header

export const Header = () => {
    let htmlString = ""
    htmlString += 
    `<header>
        <h1>!Movie Recommender!</h1>
        <h3><i>No Spoilers, We Promise</i></h3>
    </header>`
    return htmlString
}
